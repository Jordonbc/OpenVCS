[workspace]
members = [
  "Backend",
]
resolver = "2"

# optional: set which members build when you run `cargo build` at the root
default-members = ["Backend"]

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
